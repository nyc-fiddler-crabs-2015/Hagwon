<div class="container container-fit">
  <div class="row">
    <!-- Part 1 -->
    <div class="col-xs-6 col-md-6 track-left remove-right-radius" style="background-color: <%=@track.category.color%>">
      <h3><%=@track.category.name%></h3>
      <h1><%= @track.name%></h1>
      <p><%= @track.description%> </p>
      <% if @owner %>
      <i> forked from <%=link_to @owner.name, @owner%>, taken by <%=@count%> users </i>

      <% else %>
      <i> by <%=@track.owner.name%>, taken by <%=@count%> users </i>
      <%end%>


      <% if current_user && @track.user_id == current_user.id %>
      <%= link_to 'Edit', edit_track_path %>
      <%else%>
      <%= link_to 'Fork', "/tracks/#{@track.id}/fork", method: 'post'%>
      <%= link_to 'Follow', "/tracks/#{@track.id}/follow", method: 'post'%>
      <%end%>



    </div>
    <!-- Part 1 -->

    <!-- Part 2 -->
    <div class="col-xs-6 col-md-6">
      <div class="row">
      <div class="col-xs-12 col-md-12 thumbnail remove-left-radius track-right">
          <div class="text-center"><h1>Progress Stats</h1>
                    <span class="right-block">1</span> <span class="right-block">/ 14 Courses</span></div>


          <% @track.courses.each do |course|%>

          <div class="col-xs-12 col-md-12">
            <div class="row thumbnail track_courses">
              <div class="check_function col-xs-3">
                <% if current_user && course.users.find_by(id: current_user.id) %>
                <div data-id="<%= course.id %>" draggable="false" class="checked_off center-block">
                  <i class="fa fa-check check"></i>
                </div>
                <% else %>
                <div data-id="<%= course.id %>" draggable="false" class="checked_on center-block">
                  <i class="fa fa-check check"></i>
                </div>
                <% end %>

              </div>
              <div class="col-xs-9 course-box ">
                <a href="<%=course.url%>" target="_blank"><%=course.name%></a>
                <div>Rating: <%= star course.rating %></div>
                <div>Difficulty: <%= rocket course.difficulty %></div>
              </div>
            </div>
          </div>
          <%= render partial: "reviews/new", locals: {course: course, review: @review} if current_user  %>

          <% end %>
        </div>
      </div>
    </div>
    <!-- Part 2 -->
  </div>

</div>


